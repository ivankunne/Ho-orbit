# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase Edge Function secrets (keep .env.example, ignore the real one)
supabase/functions/.env
# Ook kopieën/backups — die vallen anders buiten de regel hierboven
# en bevatten wél de live sleutels.
supabase/functions/.env.*
!supabase/functions/.env.example
supabase/.temp

# Contains the plaintext master-admin password — must never enter git history
supabase/create_master_admin.sql

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
